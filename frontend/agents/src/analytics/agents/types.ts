// export type AgentBaseProps = {
//   agent_id: string;
// };

export type AddAgentProps = {}; // si agrego un agente, solo recolecto los campos base
export type SearchAgentsProps = {}; // si busco agentes, solo recolecto los campos base
export type FilterStatusAgentsProps = {}; // si filtro el estado de agentes, solo recolecto los campos base
